node_modules
dist
.angular

# 59 Mo de fixture synthetique, happee par le glob d'assets d'angular.json
# ({"glob": "**/*", "input": "public"}) : sans cette ligne elle part dans le
# contexte de build et se retrouve dans l'image.
public/fixtures/
test/fixtures/large*.json

scripts

# L'application macOS (Electron) : ni ses sources, ni ses node_modules, ni ses
# bundles n'ont leur place dans l'image.
desktop

# 1.7 Mo de captures pour le README : rien a faire dans l'image.
docs
.vscode
.claude
.git
.gitignore
*.log
start.sh
deploy.sh
Dockerfile
.dockerignore
